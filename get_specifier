/**
 * get_specifier - checks if a character is a valid specifier
 * @c: the character to check
 *
 * Return: 1 if valid specifier, 0 otherwise
 */
int get_specifier(char c)
{
    const char *valid_specifiers = "cdis";
    int i;

    for (i = 0; valid_specifiers[i]; i++)
    {
        if (valid_specifiers[i] == c)
            return 1;
    }

    return 0;
}

